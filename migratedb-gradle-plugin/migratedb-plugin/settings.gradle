buildscript {
    repositories {
        mavenLocal()
        mavenCentral()
        gradlePluginPortal()
    }
    dependencies {
        classpath group: 'org.codehaus.groovy', name:'groovy-xml', version: GroovySystem.version
    }
}
